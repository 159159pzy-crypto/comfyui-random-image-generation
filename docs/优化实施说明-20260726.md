# 前端优化实施说明(基于 2026-07-26 审查报告)

基线:app.js 取自你本机最新版(108,335 字节,含批次队列/实时预览/复现变体等 0.2.0 新功能,全部保留未动);index.html / styles.css 基于液态玻璃 v2。缓存版本已更新:`styles.css?v=20260726-liquidglass-3`、`app.js?v=20260726-hardening-1`,覆盖后 Ctrl+F5 生效。

## 已修复(报告编号 → 验证方式)

**🔴 必须修 6/6**

- #1 Esc 层级:有弹窗开着时 Esc 只关弹窗,再按才关抽屉。[实测:第一次 Esc 弹窗关/抽屉留,第二次抽屉关]
- #2 批次数量草稿:`#count` 单独绑定 input/change → schedulePersist。[实测:改 77 后草稿即存]
- #3 侧栏 "null":sidebarSection 结果 filter(Boolean) 后再 append。[实测:空 facet 分区无 "null" 字样]
- #4 作品卡逐卡毛玻璃:移除 .image-card 的 backdrop-filter,保留半透明底。[实测:computed 为 none,视觉无差]
- #5 批次数量可访问名称:小屏 label 改 sr-only 隐藏(不再 display:none)。[axe label 违规消失]
- #6 safe-area:指令台 bottom、workspace 底部留白、全屏抽屉均加 env(safe-area-inset-bottom);viewport 加 viewport-fit=cover。

**🟡 建议修 15/16**

- #7 批次竞态(补全):新增 fetchCurrentBatch 自增序号,乱序的 current 轮询响应直接丢弃;停止成功后作废在途轮询。你新版的 submitInFlight 守卫保留。
- #8 网络健壮性:request() 默认 15s 超时(AbortSignal.timeout,可被调用方 signal 覆盖);网络层异常映射为「无法连接服务器」、超时映射为「请求超时，后端未响应」。
- #9 收藏/自定义分组竞态:响应落地前校验 section 仍是 activeSection,过期响应丢弃(含 catch 路径)。
- #10 池翻页:loadPool 改为接收目标页参数,页码仅在成功后落状态;过期请求的失败不再弹 toast。
- #11 LoRA 弹窗:表单 submit preventDefault,搜索框按 Enter 不再关窗。[实测:Enter 后弹窗仍开]
- #12 姿势冲突漏报:新增 backfillPoseCache,草稿恢复的选中项不在缓存时经 /query 接口按 id 补齐(带尝试去重与在途守卫,失败静默降级为原行为)。
- #13 sticky 隐患:L107 只留 main,.mobile-tabs 定位完全交给自身 sticky 规则。
- #14 高对比:prefers-contrast 覆写选择器改 `:root, html[data-theme]`,暗色下也生效。
- #15 theme-color:头部内联脚本随主题写 meta,暗色首屏浏览器框不再闪浅色。
- #16 低透明偏好:补 .image-card 实色、.drawer-backdrop 与 dialog::backdrop 去模糊。
- #17 非法 font 简写:拆为独立属性。[实测:图标 15px/400]
- #18 焦点可见:池卡片复选框、文件选择按钮加 :has(:focus-visible) 焦点环。
- #19 无 backdrop-filter 兜底:@supports not 时玻璃面板用 --chrome-solid 实色。
- #20 触控与可读性:item-tools 24→30px、lora-actions 26→30px;8~9.5px 小字全部提到 10px;--text-tertiary 亮色加深(#6e6e73)、暗色提亮(#98989d);「管理池」等 accent-on-soft 文字新增 --accent-strong(亮 #0060C2 / 暗 #7CB8FF)达标 4.5:1。
- #21 标签页语义:HTML 初始 aria-pressed + JS 切换同步。
- #22 抽屉合成层:关闭态 visibility:hidden(延迟到位移动画结束),不再常驻 GPU 层。

**🔵 锦上添花**

- #23 唯一 h1(作品库降 h2,选择器同步)。#24 指令台改带 aria-label 的 section(region landmark)。#25 顶栏时代遗留规则清理(batch-summary max-width/justify-self、quantity-field 82px)。#17b --accent-hover 接入主按钮 hover。#26 移动端标签条与顶栏 6px 缝隙闭合(top 减 6px,实测 0px)。#27 非标字重 570→500、620→600。#28 URL 拼接统一 encodeURIComponent。#31 persistFavoriteTree 补 try/catch。#32 部分:applyTheme 空值防护、抽取数量越界回写、isFavorite 死代码移除。
- 上次对齐检查两项:抽取数量行提示居中(偏移 0px)、移动端主题钮恢复 36px 与徽章一致。

## 未动(建议由迭代 app.js 的会话处理)

- #29 全量重渲染改增量:涉及列表渲染架构,与队列/预览新功能改动面重叠,单独做容易冲突。
- #30 「全选词库」计数漂移:准确修需要后端提供实时计数,前端硬调会引入新的不一致。
- #32 剩余:favoriteCount 改名、重复集合运算提取 helper、gender 出入参枚举统一(涉后端)。
- 修复中发现的连带回归已处理:标签 chips 提字号后被 max-height 裁切 → 42px 完整两行。

## 回归验证

无头 Chrome + 全套接口桩:桌面亮/暗、移动端、抽屉打开态 axe-core 扫描全部 0 违规;全程零 console error / pageerror;上述每个 [实测] 项由修复前失败的同一脚本翻绿确认;三个宽度对齐测量无 >2px 偏差;桌面/暗色/移动/抽屉四张截图人工核对无视觉回归。
